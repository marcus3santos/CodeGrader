;; q1.lisp

(deftest test-next-state ()
    (check
      (equal (let ((std  '((1 . 2) (2 . (3 4)) (3 . nil) (4 . nil))))
	       (next-state std 2))
	     '(3 4))
      (equal (let ((std  '((1 . 2) (2 . (3 4)) (3 . nil) (4 . nil))))
	       (next-state std 1))
	     2)
      (equal (let ((std  '((1 . 2) (2 . (3 4)) (3 . nil) (4 . nil))))
	       (next-state std 3))
	     nil)
      (equal (let ((std  '((start . test1)    
                           (test1 . (init end1))
                           (init . test2)
                           (test2 . (bdy return))
                           (bdy . incr)
                           (incr . test2)
                           (return . end1)
                           (end1 . nil))))
	       (next-state std 'init))
	     'test2)))

(deftest test-jump-state ()
    (check
      (equal (let ((std '((start . test1)    
			  (test1 . (init end1))
			  (init . test2)
			  (test2 . (bdy return))
			  (bdy . incr)
			  (incr . test2)
			  (return . end1)
			  (end1 . nil))))
	       (jump-state std 'init 'test1))
	     'test2)
      (equal (let ((std '((start . test1)    
			  (test1 . (init end1))
			  (init . test2)
			  (test2 . (bdy return))
			  (bdy . incr)
			  (incr . test2)
			  (return . end1)
			  (end1 . nil))))
	       (jump-state std 'test2 'incr))
	     '(bdy return))
       (equal (let ((std '((start . test1)    
			  (test1 . (init end1))
			  (init . test2)
			  (test2 . (bdy return))
			  (bdy . incr)
			  (incr . test2)
			  (return . end1)
			  (end1 . nil))))
		(jump-state std nil 'return))
	      'return)
      (equal (let ((std '((1 . 2)    
			  (2 . (3 4))
			  (3 . nil)
			  (4 . nil))))
	       (jump-state std 3 2))
	     2)))

(deftest test-next-fork ()
  (check
    (equal (let ((std '((1 . 2)    
			(2 . (3 4))
			(3 . nil)
			(4 . nil))))
	     (next-fork std 1))
	   '(3 4))
    (equal (let ((std '((1 . 2)    
			(2 . (3 4))
			(3 . nil)
			(4 . nil))))
	     (next-fork std 3))
	   nil)
    (equal (let ((std '((1 . 2)    
			(2 . (3 4))
			(3 . 1)
			(4 . nil))))
	     (next-fork std 3))
	   '(3 4))
    (equal (let ((std '((start . test1)    
			(test1 . (init end1))
			(init . test2)
			(test2 . (bdy return))
			(bdy . incr)
			(incr . test2)
			(return . end1)
			(end1 . nil))))
	     (next-fork std 'bdy))
	   '(bdy return))))

(defun test-q1 ()
  (test-next-fork)
  (test-jump-state)
  (test-next-state)
  (fmakunbound 'jump-state)
  (fmakunbound 'next-state)
  (fmakunbound 'next-fork))

(test-q1)
